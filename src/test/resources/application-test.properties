# Configuração de teste com H2 em memória
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.type.preferred_enum_type=STRING

# Configurações de segurança para teste
api.security.token.secret=test-secret-key-for-unit-tests

# Logging
logging.level.com.desafio.livraria=DEBUG
logging.level.org.springframework.security=DEBUG
